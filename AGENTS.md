# AGENTS.md

## 文档导航

- 根文档：项目总览、边界、跨端约定。
- `frontend/AGENTS.md`：Android 前端约束。
- `frontend/PLAN.md`：前端计划。
- `frontend/PROGRESS.md`：前端进展。
- `backend/AGENTS.md`：Python 后端约束。
- `backend/PLAN.md`：后端计划。
- `backend/PROGRESS.md`：后端进展。

## 项目目标

本分支聚焦“题集”能力，目标是把拍题解析、用户、家庭、多孩子、错题收集、关注题和存储迁移能力做成长期可扩展的基础。

## 核心原则

- 家庭是租户边界。
- 孩子是学习数据主体。
- 错题以独立小题为单位。
- SQLite 作为第一阶段数据库。
- 图片和附件存文件系统，数据库只存元数据和 `storage_key`。
- 存储层要抽象，后续可无缝迁移到 OSS。
- 表结构和 ID 设计要兼容后续迁移到 PostgreSQL。

## 关键约定

- `tenant_id` 必须贯穿所有业务表和访问控制。
- `student_id` 必须区分家庭中的不同孩子。
- 原图、裁剪图、头像等都进入统一资产表。
- 业务表不直接存绝对磁盘路径。
- 图片目录不按日期分层，日期只进数据库字段。

## 当前阶段重点

- 登录注册
- 家庭租户
- 家庭成员
- 孩子管理
- SQLite 数据库
- 统一资产表
- 解析任务落库
- 题块集合：错题集与关注集

账号约定：

- 注册和登录只使用 `username` + `password`。
- 不要求手机号、邮箱、验证码或家庭名称。
- 注册成功后后端自动创建家庭租户，默认家庭名为“用户名的家庭”。
- Bearer token 默认有效期为 10 年，可通过后端环境变量覆盖。

## 产品导航

- 主导航固定为：`拍题`、`题集`、`设置`。
- 任务列表不作为主导航，放在拍题页左上角入口。
- 题集页包含两个集合：`错题集`、`关注`。
- 设置页靠上位置展示当前孩子，并提供切换入口。
- 切换孩子后，拍题新任务、任务列表、题集、题块集合操作都切换到当前孩子。

## 题块集合

- 每个题块卡片提供两个按钮：`错题`、`关注`。
- 错题可以由批改结果自动加入，也可以手动加入 / 移出。
- 关注只由用户手动加入 / 取消。
- 同一道题块可以同时存在于错题集和关注集。
- 题目切图本阶段只预留数据结构，不实现具体裁剪算法。
